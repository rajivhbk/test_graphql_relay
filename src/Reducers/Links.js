import { combineReducers } from 'redux';

function LinksList (state = [], action) {
    switch (action.type) {
        default:
        return state;
    }
}

export const Links = combineReducers({
    LinksList
});